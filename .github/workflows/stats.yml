name: Generate Statistics

on:
  schedule:
    - cron: "0 0 * * *" # Daily at midnight UTC
  push:
    branches: [main]
    paths:
      - "Casks/**"
      - "Formula/**"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate-stats:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Generate statistics
        env:
          # GITHUB_TOKEN provides push access to this repo's traffic.
          # For comparison tap traffic (ublue-os/*), a PAT with push access
          # to those repos must be stored as GH_TRAFFIC_TOKEN secret.
          GITHUB_TOKEN: ${{ secrets.GH_TRAFFIC_TOKEN || secrets.GITHUB_TOKEN }}
        run: |
          ./tap-tools/tap-stats generate --output docs/ \
            --compare ublue-os/homebrew-tap \
            --compare ublue-os/homebrew-experimental-tap

      - name: Commit generated files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/stats.json docs/index.html
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "chore(stats): update tap statistics [skip ci]"
            git push
          fi
